mod systems {
    mod arena;
    mod gov;
    mod matchmaking;
    mod gacha;
    mod teambuilder;
}

mod models {
    mod game;
    mod global;
    mod player;
    mod position;
    mod pool;
    mod piece;
    mod team;
}

mod pieces {
    mod pieces;
    mod a;
    mod b;
}

mod consts;

mod types;

mod tests {
    mod test_world;
}
