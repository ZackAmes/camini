mod systems {
    mod arena;
    mod gov;
    mod matchmaking;
    mod gacha;
    mod teambuilder;
}

mod models {
    mod game;
    mod global;
    mod player;
    mod position;
    mod pool;
    mod piece;
    mod team;
}

mod pieces {
    mod pieces;
 //   mod piececomponent;
    mod a;
    mod b;
}

mod effects {
    mod effects;
    mod damage;
}
mod consts;

mod types;

mod tests {
    mod test_world;
}
