mod systems {
    mod actions;
    mod gov;
    mod matchmaking;
    mod gacha;
}

mod models {
    mod game;
    mod global;
    mod player;
    mod position;
    mod pool;
    mod piece;
}

mod pieces {
    mod pieces;
    mod a;
    mod b;
}

mod consts;

mod tests {
    mod test_world;
}
